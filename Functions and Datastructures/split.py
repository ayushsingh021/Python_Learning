# You call the input() function to take input from the user. This function reads a line of input from standard input (stdin).

# input().split() reads the line of input and then splits it into substrings based on whitespace characters.

# The result is a list containing the substrings obtained after splitting the input string.

# For example:

# python
# Copy code
# user_input = input("Enter some words separated by spaces: ")
# word_list = user_input.split()

# print(word_list)
# If you input "apple banana orange" when prompted, the output will be:

# css
# Copy code
# ['apple', 'banana', 'orange']